import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'resume.com.app',
  appName: 'resume',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
